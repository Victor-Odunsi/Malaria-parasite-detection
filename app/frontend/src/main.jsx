import React from 'react';
import ReactDOM from 'react-dom/client';
import MalariaScope from './App';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <MalariaScope />
  </React.StrictMode>
);
